<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.karino2.kakioku">

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KaKioku">
        <activity
            android:name=".DeckListActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KaKioku.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QAActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.KaKioku.NoActionBar">
        </activity>
        <activity
            android:name=".AddCardActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.KaKioku.NoActionBar">
        </activity>
        <activity android:name=".CardListActivity" android:label="@string/app_name" android:theme="@style/Theme.KaKioku.NoActionBar" />
        <activity
            android:name=".EditCardActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.KaKioku.NoActionBar">
        </activity>

    </application>

</manifest>